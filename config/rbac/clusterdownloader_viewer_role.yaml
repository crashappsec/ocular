# This rule is not used by the project ocular itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to ocular.crashoverride.run resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: ocular
    app.kubernetes.io/managed-by: kustomize
  name: clusterdownloader-viewer-role
rules:
- apiGroups:
  - ocular.crashoverride.run
  resources:
  - clusterdownloaders
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ocular.crashoverride.run
  resources:
  - clusterdownloaders/status
  verbs:
  - get
